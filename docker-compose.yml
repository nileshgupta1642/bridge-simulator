services:
  bridge-api:
    build: .
    ports:
      - "8001:8001"

  bridge-web:
    image: python:3.14-slim
    working_dir: /app/web
    volumes:
      - ./web:/app/web
    ports:
      - "8000:8000"
    command: python -m http.server 8000 --bind 0.0.0.0